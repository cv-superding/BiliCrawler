# B站弹幕与评论数据爬取工具 - 依赖清单
# 仅依赖轻量、成熟的标准库友好型第三方库

requests>=2.31.0      # HTTP 客户端（含会话、重试、代理）
pyyaml>=6.0           # YAML 配置文件解析
flask>=3.0.0          # Web 控制面板（轻量后端）
werkzeug>=3.0.0       # WSGI 工具（Flask 依赖）
